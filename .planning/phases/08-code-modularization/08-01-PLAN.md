---
phase: 08-code-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/utils.js
  - css/shared.css
  - css/admin.css
  - css/agent.css
  - css/client.css
  - admin.html
  - agent.html
  - client.html
autonomous: true
requirements: [CODE-01, CODE-05, CODE-06]

must_haves:
  truths:
    - "All three role pages load shared utilities from lib/utils.js"
    - "All three role pages load CSS from external files instead of inline style blocks"
    - "sanitize(), fmt(), timeAgo(), showToast(), openModal(), closeModal() are available as window.BX.* AND as bare globals"
    - "Visual appearance of all pages is identical before and after CSS extraction"
  artifacts:
    - path: "lib/utils.js"
      provides: "Shared utility functions under window.BX namespace"
      exports: ["BX.sanitize", "BX.fmt", "BX.timeAgo", "BX.showToast", "BX.openModal", "BX.closeModal"]
    - path: "css/shared.css"
      provides: "Common reset, fonts, toast, modal, auth-gate, session-warning styles"
      min_lines: 30
    - path: "css/admin.css"
      provides: "Admin-specific CSS (sidebar, dashboard, tables, forms, settings)"
      min_lines: 100
    - path: "css/agent.css"
      provides: "Agent-specific CSS (sidebar, clients, pnl, settlement, mobile responsive)"
      min_lines: 100
    - path: "css/client.css"
      provides: "Client-specific CSS (bottom nav, bet slip, markets, portfolio, history)"
      min_lines: 100
  key_links:
    - from: "admin.html"
      to: "lib/utils.js"
      via: "script src tag in head"
      pattern: '<script src="lib/utils.js">'
    - from: "agent.html"
      to: "lib/utils.js"
      via: "script src tag in head"
      pattern: '<script src="lib/utils.js">'
    - from: "client.html"
      to: "lib/utils.js"
      via: "script src tag in head"
      pattern: '<script src="lib/utils.js">'
    - from: "admin.html"
      to: "css/shared.css"
      via: "link rel stylesheet in head"
      pattern: '<link rel="stylesheet" href="css/shared.css">'
---

<objective>
Extract shared utilities into lib/utils.js and all CSS into external files.

Purpose: Establish the shared foundation (per D-01, D-02, D-09, D-10, D-11) that Wave 2 plans depend on. CSS extraction is visual-only (zero logic risk). Utils extraction replaces duplicated functions with a single source of truth.

Output: lib/utils.js, css/shared.css, css/admin.css, css/agent.css, css/client.css. HTML files updated with link/script tags.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-code-modularization/08-CONTEXT.md

@admin.html (CSS: lines 10-253, shared utils: lines 1417-1433, 3903-3920)
@agent.html (CSS: lines 10-289, shared utils: lines 923-938)
@client.html (CSS: lines 10-381, shared utils: lines 635-641, 2148-2160)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/utils.js with window.BX namespace and extract all CSS into external files</name>
  <files>lib/utils.js, css/shared.css, css/admin.css, css/agent.css, css/client.css</files>
  <read_first>
    - admin.html lines 10-253 (admin CSS)
    - admin.html lines 1417-1433 (sanitize, timeAgo)
    - admin.html lines 3903-3920 (openModal, closeModal, showToast)
    - agent.html lines 10-289 (agent CSS)
    - agent.html lines 923-938 (sanitize, timeAgo, fmt, showToast, openModal, closeModal)
    - client.html lines 10-381 (client CSS)
    - client.html lines 635-641 (sanitize, fmt)
    - client.html lines 2148-2160 (showToast)
  </read_first>
  <action>
**Create `lib/` and `css/` directories.**

**Create `lib/utils.js` (per D-01, D-11):**

Build a `window.BX` namespace object containing these shared functions:

1. `BX.sanitize(str)` -- Canonical implementation using DOM textContent approach (from admin.html line 1417). Handles null/undefined.
2. `BX.fmt(n, d=2)` -- Number formatter from agent.html line 931: `parseFloat(n||0).toLocaleString(undefined, {minimumFractionDigits:d, maximumFractionDigits:d})`. Note: client.html uses `minimumFractionDigits:0` variant — keep the agent version as canonical (d=2 default) since callers can pass d=0 when needed.
3. `BX.timeAgo(ts)` -- From admin.html lines 1424-1433. Returns HTML span with color coding.
4. `BX.showToast(msg, type='info')` -- Use the admin.html version (lines 3912-3919) which includes emoji icons and fade animation. Targets `#toast-container`.
5. `BX.openModal(id)` -- `document.getElementById(id).classList.add('open')` (from admin.html line 3903).
6. `BX.closeModal(id)` -- `document.getElementById(id).classList.remove('open')` (from admin.html line 3906).

**CRITICAL: Global aliases for onclick handlers.** After defining `window.BX`, create bare global aliases so HTML onclick handlers continue working without namespace prefix:

```javascript
// Global aliases — HTML onclick handlers reference bare function names
window.sanitize = BX.sanitize;
window.fmt = BX.fmt;
window.timeAgo = BX.timeAgo;
window.showToast = BX.showToast;
window.openModal = BX.openModal;
window.closeModal = BX.closeModal;
```

**Create CSS files (per D-09, D-10):**

Identify shared CSS patterns that appear in 2+ files and extract to `css/shared.css`:
- Box-sizing reset (`*, *::before, *::after`)
- Body base (font-family: Inter, background: #0f172a, color: #e2e8f0)
- Scrollbar styles
- `.btn` variants (btn-primary, btn-ghost, btn-danger, btn-success, btn-sm, btn-lg)
- `.card`, `.card-header`, `.card-title` styles
- `.modal`, `.modal-box`, `.modal-close` styles
- `.toast`, `#toast-container` styles
- `.badge` variants
- `.form-group`, `.form-input`, `.form-select` styles
- `.mono` (JetBrains Mono font)
- `.text-muted`, `.text-white` helper classes
- `.empty-state` / `.empty` patterns
- `@keyframes authSpin` animation
- `#authGate` and `#session-warning-banner` styles (shared auth gate pattern)
- Google Fonts `@import` is NOT needed (already loaded via `<link>` in HTML head)

Extract role-specific CSS to their respective files:
- `css/admin.css`: Sidebar layout, nav-item, admin-specific card styles, dashboard grid, user tables, market cards, settlement, audit log, balance sheet, payout preview, risk tab, sim toggle, platform config. Lines 10-253 from admin.html minus shared patterns.
- `css/agent.css`: Sidebar, nav-item, agent-specific cards, client list, P&L report styles (`.pnl-detail-row`, `.pnl-toggle`), settlement cards, mobile responsive overrides (`@media`), bottom nav mobile, more-menu. Lines 10-289 from agent.html minus shared patterns.
- `css/client.css`: Bottom nav, mobile layout, bet slip, market cards, match group, fancy cards, portfolio, history, pull-to-refresh, offline banner, filter pills, account tab, event book bar. Lines 10-381 from client.html minus shared patterns.

Note: When splitting, if a style exists in one file but is used by the shared utility (e.g., toast styles), put it in shared.css. When in doubt, keep in role-specific file (can be moved later).

Also move the small `<style>@keyframes authSpin{...}</style>` block (admin.html line 266, agent.html line 304) into shared.css.
  </action>
  <verify>
    <automated>test -f lib/utils.js && test -f css/shared.css && test -f css/admin.css && test -f css/agent.css && test -f css/client.css && grep -q "window.BX" lib/utils.js && grep -q "BX.sanitize" lib/utils.js && grep -q "BX.fmt" lib/utils.js && grep -q "BX.timeAgo" lib/utils.js && grep -q "BX.showToast" lib/utils.js && grep -q "BX.openModal" lib/utils.js && grep -q "window.sanitize = BX.sanitize" lib/utils.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - lib/utils.js exists with window.BX namespace containing sanitize, fmt, timeAgo, showToast, openModal, closeModal
    - lib/utils.js has global aliases: window.sanitize, window.fmt, window.timeAgo, window.showToast, window.openModal, window.closeModal
    - css/shared.css exists with common reset, fonts, buttons, cards, modals, toasts, forms, authSpin keyframe
    - css/admin.css exists with admin-specific sidebar, dashboard, tables, markets styles
    - css/agent.css exists with agent-specific sidebar, clients, P&L, settlement, mobile styles
    - css/client.css exists with client-specific bottom nav, bet slip, markets, portfolio styles
  </acceptance_criteria>
  <done>All 5 new files created with correct content. BX namespace has 6 functions. Global aliases defined.</done>
</task>

<task type="auto">
  <name>Task 2: Update HTML files to use external CSS and lib/utils.js</name>
  <files>admin.html, agent.html, client.html</files>
  <read_first>
    - admin.html lines 1-10 (head section)
    - admin.html lines 250-266 (end of style, authSpin)
    - admin.html lines 1417-1433 (sanitize, timeAgo to remove)
    - admin.html lines 3903-3921 (openModal, closeModal, showToast to remove)
    - agent.html lines 1-10 (head section)
    - agent.html lines 286-304 (end of style, authSpin)
    - agent.html lines 923-938 (utils to remove)
    - client.html lines 1-10 (head section)
    - client.html lines 378-386 (end of style)
    - client.html lines 635-641 (sanitize, fmt to remove)
    - client.html lines 2148-2162 (showToast to remove)
  </read_first>
  <action>
**Update all three HTML `<head>` sections.** Replace the inline `<style>...</style>` block with external CSS links + utils script. The head should become:

```html
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="..."> <!-- keep existing viewport -->
  <title>...</title> <!-- keep existing title -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="css/shared.css">
  <link rel="stylesheet" href="css/{role}.css">
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="auth.js"></script>
  <script src="lib/utils.js"></script>
</head>
```

Where `{role}` is `admin`, `agent`, or `client` respectively.

**Remove inline `<style>` blocks entirely** from all three files (the CSS content is now in external files).

**Remove the `<style>@keyframes authSpin{...}</style>` tag** from admin.html (line 266) and agent.html (line 304) — this is now in shared.css.

**Remove duplicate utility functions from inline `<script>` blocks:**

In admin.html: Remove `function sanitize(str)` (lines 1417-1421), `function timeAgo(ts)` (lines 1424-1433), `function openModal(id)` (lines 3903-3905), `function closeModal(id)` (lines 3906-3908), `function showToast(msg, type)` (lines 3912-3920), and the comments around them. Keep everything else in the inline script.

In agent.html: Remove `function sanitize(s)` (line 923), `function timeAgo(ts)` (lines 924-929), `function fmt(n,d)` (line 931), `function showToast(msg, type)` (lines 932-936), `function openModal(id)` (line 937), `function closeModal(id)` (line 938). Keep everything else in the inline script.

In client.html: Remove `function sanitize(str)` (lines 635-638), `function fmt(n, d)` (lines 640-642), `function showToast(msg, type)` (lines 2148-2159). Keep everything else in the inline script.

**IMPORTANT:** Do NOT remove the `const fmt` local variable inside `renderBalanceSheet()` in admin.html (line 2169) — that is a LOCAL const that shadows the global, and it has a specific 2-decimal formatter. Similarly, do NOT remove `const fmtAmt` locals (lines 2855, 2891).

**IMPORTANT:** Do NOT touch `auth.js` — it is already external and loads before utils.

**IMPORTANT:** The inline `<script>` blocks still remain (they contain the role-specific JS). Only the shared utility FUNCTIONS are removed. The global state variables (`const sb`, `let allUsers`, etc.) and all role-specific functions stay in the inline script for now — those get extracted in Plans 02 and 03.
  </action>
  <verify>
    <automated>grep -q 'css/shared.css' admin.html && grep -q 'css/admin.css' admin.html && grep -q 'lib/utils.js' admin.html && grep -q 'css/shared.css' agent.html && grep -q 'css/agent.css' agent.html && grep -q 'lib/utils.js' agent.html && grep -q 'css/shared.css' client.html && grep -q 'css/client.css' client.html && grep -q 'lib/utils.js' client.html && ! grep -q '<style>' admin.html && ! grep -q '<style>' agent.html && ! grep -q '<style>' client.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 HTML files have `<link rel="stylesheet" href="css/shared.css">` in head
    - All 3 HTML files have role-specific CSS link (css/admin.css, css/agent.css, css/client.css)
    - All 3 HTML files have `<script src="lib/utils.js"></script>` in head after auth.js
    - No `<style>` tags remain in any HTML file
    - Inline `<script>` blocks remain but shared util functions (sanitize, fmt, timeAgo, showToast, openModal, closeModal) are removed from them
    - Local `const fmt` inside renderBalanceSheet (admin.html) is preserved
    - admin.html, agent.html, client.html load correctly in browser with `python3 -m http.server 3000` (visual parity)
  </acceptance_criteria>
  <done>All 3 HTML files reference external CSS and lib/utils.js. No inline style blocks. Duplicate utility functions removed from inline scripts. Pages render identically.</done>
</task>

</tasks>

<verification>
1. Run `python3 -m http.server 3000` and visually confirm admin.html, agent.html, client.html render correctly
2. Open browser console — no "function not defined" errors for sanitize, fmt, timeAgo, showToast, openModal, closeModal
3. Verify `window.BX.sanitize` exists in console
4. Verify `window.sanitize` exists in console (global alias)
5. Click a button that calls showToast — toast appears correctly
6. Open and close a modal — modal behavior works
</verification>

<success_criteria>
- 5 new files created (lib/utils.js, css/shared.css, css/admin.css, css/agent.css, css/client.css)
- All 3 HTML files reference external CSS and utils.js
- No inline `<style>` blocks remain
- Shared utility functions removed from inline scripts (deduplicated)
- Visual appearance identical before and after
- No JS console errors on page load
</success_criteria>

<output>
After completion, create `.planning/phases/08-code-modularization/08-01-SUMMARY.md`
</output>

---
phase: 06-agent-p-l-views
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [agent.html, admin.html]
autonomous: true
requirements: [APNL-10, APNL-11]

must_haves:
  truths:
    - "Agent sees live estimated P&L exposure during open markets, clearly labeled as Estimated"
    - "Admin agent settlement cards display share-adjusted P&L from settlement_results"
  artifacts:
    - path: "agent.html"
      provides: "Live exposure section in P&L tab with Estimated label"
      contains: "Estimated"
    - path: "admin.html"
      provides: "Share-adjusted P&L rows in settlement cards from settlement_results"
      contains: "settlement_results"
  key_links:
    - from: "agent.html renderPnL()"
      to: "allOrders (open orders)"
      via: "client-side exposure computation from open orders with partnership_share"
      pattern: "OPEN.*exposure"
    - from: "admin.html renderSettlement()"
      to: "settlement_results table"
      via: "sb.from('settlement_results') query in refreshData"
      pattern: "allSettlementResults"
---

<objective>
Add live P&L exposure tracking for agents during open markets, and enhance admin settlement cards with share-adjusted P&L from settlement_results.

Purpose: Agents need to see their estimated exposure while markets are open (not just after settlement). Admins need to see each agent's actual partnership-adjusted P&L in settlement cards rather than just raw chip flow.

Output: Live exposure section in agent.html P&L tab, share-adjusted P&L rows in admin.html settlement cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-p-l-views/06-CONTEXT.md
@.planning/phases/06-agent-p-l-views/06-01-SUMMARY.md

<interfaces>
<!-- settlement_results table columns -->
event_id, agent_id, total_client_pnl, total_commission_paid,
agent_pnl_share, agent_commission_share, agent_net_pnl,
partnership_share_at_settlement, settled_at

<!-- From Plan 06-01: agent.html now has -->
let allSettlementResults = [];  // loaded in refreshData(), filtered by agent_id
renderPnL() — rewritten to use allSettlementResults for settled data

<!-- agent.html global state -->
currentUser.partnership_share — agent's partnership % (for live estimate)
allOrders — client orders (has .status, .total_cost, .shares, .outcome_id, .user_id)
allEvents — events (has .status: OPEN/SUSPENDED/SETTLED/VOID, .winning_outcome)
allOutcomes — outcomes (has .event_id, .current_price)
myClients — agent's clients

<!-- admin.html existing patterns -->
renderSettlement() — builds settlement cards (lines 3176-3252)
refreshData() — loads allUsers, allTransactions, allEvents, allOutcomes, allOrders (lines 1434-1458)
.settlement-card, .sc-agent, .sc-name, .sc-row, .sc-net — existing CSS classes
fmt(n, decimals) available in agent.html, admin uses toLocaleString directly

<!-- Refresh interval -->
agent.html: setInterval(refreshData, 30000) — 30s polling already exists
admin.html: setInterval(refreshData, 30000) — 30s polling already exists
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live P&L exposure section to agent P&L tab</name>
  <files>agent.html</files>
  <read_first>
    - agent.html lines 439-480 (P&L tab HTML as modified by Plan 01)
    - agent.html lines 916-937 (dashboard open exposure computation — reuse pattern)
    - agent.html lines 1186-1251 (renderPnL as modified by Plan 01)
  </read_first>
  <action>
**Per D-08, D-09, D-10 — live exposure from open orders, labeled "Estimated".**

1. Add a new HTML section in the P&L tab, ABOVE the settled stats grid (before the stats-grid-4 div). This separates live from settled data per D-10:

```html
<div class="card" id="pnlLiveSection" style="margin-bottom:20px;">
  <div class="card-header">
    <div class="card-title">Live Exposure <span class="badge" style="background:rgba(251,191,36,0.15);color:#fbbf24;font-size:0.7rem;margin-left:8px;">ESTIMATED</span></div>
  </div>
  <div class="stats-grid stats-grid-3" style="margin-bottom:0;">
    <div class="stat-card" style="border-left:3px solid #fbbf24;">
      <div class="stat-label">Open Market Exposure</div>
      <div class="stat-value" style="color:#fbbf24;">🪙 <span id="pnlLiveExposure">0</span></div>
      <div class="stat-change">Potential payout on open orders</div>
    </div>
    <div class="stat-card" style="border-left:3px solid #fbbf24;">
      <div class="stat-label">Est. Agent Exposure</div>
      <div class="stat-value" style="color:#fbbf24;">🪙 <span id="pnlLiveAgentExposure">0</span></div>
      <div class="stat-change">At <span id="pnlLiveSharePct">0</span>% partnership share</div>
    </div>
    <div class="stat-card" style="border-left:3px solid #fbbf24;">
      <div class="stat-label">Open Markets</div>
      <div class="stat-value" style="color:#fbbf24;"><span id="pnlLiveMarkets">0</span></div>
      <div class="stat-change">Markets with active client bets</div>
    </div>
  </div>
</div>
```

2. In renderPnL(), add live exposure computation (at the top, before settled data):

```javascript
// ── Live Exposure (D-08, D-09, D-10) ──
const openEvents = allEvents.filter(e => e.status === 'OPEN' || e.status === 'SUSPENDED');
const openEventIds = new Set(openEvents.map(e => e.id));
const openOutcomeIds = new Set(allOutcomes.filter(o => openEventIds.has(o.event_id)).map(o => o.id));
const openOrds = allOrders.filter(o => o.status === 'OPEN' && openOutcomeIds.has(o.outcome_id));

// Total client exposure on open markets = sum of shares (potential payout)
const liveExposure = openOrds.reduce((s, o) => s + parseFloat(o.shares || 0), 0);
const pShare = parseFloat(currentUser.partnership_share || 0);
const agentLiveExposure = liveExposure * pShare / 100;

// Count distinct open markets with client orders
const openMarketsWithBets = new Set(allOutcomes.filter(o => openOutcomeIds.has(o.id) && openOrds.some(ord => ord.outcome_id === o.id)).map(o => o.event_id)).size;

document.getElementById('pnlLiveExposure').textContent = fmt(liveExposure, 0);
document.getElementById('pnlLiveAgentExposure').textContent = fmt(agentLiveExposure, 0);
document.getElementById('pnlLiveSharePct').textContent = pShare;
document.getElementById('pnlLiveMarkets').textContent = openMarketsWithBets;

// Hide live section if no open markets with bets
document.getElementById('pnlLiveSection').style.display = openMarketsWithBets > 0 ? '' : 'none';
```

3. Add a clear visual separator between the live section and settled section. Above the settled stats grid, add:
```html
<div class="page-header" style="margin-top:8px;">
  <div><div class="card-title">Settled P&L <span class="badge" style="background:rgba(16,185,129,0.15);color:#10b981;font-size:0.7rem;margin-left:8px;">ACTUAL</span></div></div>
</div>
```

4. The existing 30s polling via `setInterval(refreshData, 30000)` (per D-09) already refreshes allOrders and allEvents, which triggers renderPnL() via renderActiveTab(). No additional subscription needed — the polling handles live updates.
  </action>
  <verify>
    <automated>grep -c 'pnlLiveExposure\|pnlLiveAgentExposure\|ESTIMATED\|pnlLiveSection' agent.html | xargs test 4 -le</automated>
  </verify>
  <acceptance_criteria>
    - agent.html has a "Live Exposure" card section with ESTIMATED badge above settled data
    - Three live stat cards: Open Market Exposure, Est. Agent Exposure (with partnership %), Open Markets count
    - Live exposure computed from open orders filtered to open/suspended events
    - Agent exposure = total exposure * partnership_share / 100
    - Live section hidden when no open markets have client bets
    - Settled section labeled with ACTUAL badge
    - 30s polling refreshes live data automatically (existing setInterval)
  </acceptance_criteria>
  <done>Agent sees live estimated P&L exposure during open markets, clearly labeled "Estimated" and visually separated from "Actual" settled data</done>
</task>

<task type="auto">
  <name>Task 2: Add share-adjusted P&L to admin settlement cards</name>
  <files>admin.html</files>
  <read_first>
    - admin.html lines 1434-1458 (refreshData — need to add settlement_results query)
    - admin.html lines 3176-3252 (renderSettlement — add share-adjusted rows to cards)
    - admin.html lines 210-221 (settlement card CSS)
  </read_first>
  <action>
**Per D-11, D-12, D-13 — share-adjusted P&L in admin settlement cards from settlement_results.**

1. Add `let allSettlementResults = [];` to admin.html global variable declarations (near other `let allUsers = [];` etc declarations).

2. In admin.html `refreshData()` (around line 1436), add settlement_results to the Promise.all:
   - Add to the array: `sb.from('settlement_results').select('*')`
   - Destructure the result: add `sr` to the destructured array
   - Add: `allSettlementResults = sr.data || [];`

3. In `renderSettlement()` (line 3180, inside the `cards` map), after computing the existing chip-flow variables (deposited, recovered, settled, outstanding, etc.) and BEFORE the return template literal:

```javascript
// Share-adjusted P&L from settlement_results (per D-11, D-12)
const agentSR = allSettlementResults.filter(r => r.agent_id === agent.id);
const srPnlShare = agentSR.reduce((s, r) => s + parseFloat(r.agent_pnl_share || 0), 0);
const srCommShare = agentSR.reduce((s, r) => s + parseFloat(r.agent_commission_share || 0), 0);
const srNetPnl = agentSR.reduce((s, r) => s + parseFloat(r.agent_net_pnl || 0), 0);
const srAvgShare = agentSR.length > 0
  ? (agentSR.reduce((s, r) => s + parseFloat(r.partnership_share_at_settlement || 0), 0) / agentSR.length)
  : parseFloat(agent.partnership_share || 0);
```

4. In the settlement card template literal (line 3217-3238), add share-adjusted P&L rows AFTER the "Gross Exposure" row and BEFORE the "Cash settled" row. Insert inside the template:

```html
${agentSR.length > 0 ? `
  <div class="sc-row" style="border-top:1px solid #334155;margin-top:6px;padding-top:6px;">
    <span class="lbl" style="color:#a78bfa;font-weight:600;">P&L Share (${srAvgShare.toFixed(0)}%)</span>
    <span class="val" style="color:${srPnlShare >= 0 ? '#10b981' : '#ef4444'};">🪙 ${srPnlShare.toLocaleString(undefined,{minimumFractionDigits:2})}</span>
  </div>
  <div class="sc-row">
    <span class="lbl" style="color:#a78bfa;">Commission Cost</span>
    <span class="val" style="color:#a78bfa;">🪙 ${srCommShare.toLocaleString(undefined,{minimumFractionDigits:2})}</span>
  </div>
  <div class="sc-row">
    <span class="lbl" style="color:#a78bfa;font-weight:600;">Agent Net P&L</span>
    <span class="val" style="color:${srNetPnl >= 0 ? '#10b981' : '#ef4444'};font-weight:700;">🪙 ${srNetPnl.toLocaleString(undefined,{minimumFractionDigits:2})}</span>
  </div>
` : ''}
```

This adds three purple-themed rows showing the agent's actual economics per D-13 (keep existing sc-row pattern). The rows only appear if settlement_results exist for that agent (avoids showing empty zeros before any markets settle).

5. Add a "Markets Settled" count to the card header area (in the sc-name div), appending after the existing "X clients" text:
```
 · ${agentSR.length} settled
```
  </action>
  <verify>
    <automated>grep -c 'allSettlementResults\|settlement_results\|srPnlShare\|srNetPnl\|P&L Share' admin.html | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - admin.html declares allSettlementResults global variable
    - admin.html refreshData() queries settlement_results table (no filter — admin sees all)
    - renderSettlement() computes per-agent P&L share, commission cost, and net P&L from settlement_results
    - Settlement cards show three purple-colored rows: P&L Share (with %), Commission Cost, Agent Net P&L
    - Share-adjusted rows only appear when settlement_results exist for that agent
    - Positive values green, negative values red, commission values purple (#a78bfa)
    - Card header shows count of settled markets per agent
  </acceptance_criteria>
  <done>Admin settlement cards display share-adjusted P&L figures reflecting each agent's actual economics from settlement_results</done>
</task>

</tasks>

<verification>
1. Open agent.html, log in as agent with open market bets
2. Navigate to P&L tab — verify "Live Exposure" section appears with ESTIMATED badge
3. Verify live section shows open market exposure, agent exposure at partnership %, and market count
4. Verify settled section shows ACTUAL badge and uses real settlement_results data
5. Close all markets — verify live section disappears
6. Open admin.html, navigate to Agent Settlement tab
7. Verify settlement cards now show purple P&L Share, Commission Cost, and Agent Net P&L rows
8. Verify the share-adjusted figures match what agents see in their own P&L tab
</verification>

<success_criteria>
- Agent live exposure visible during open markets with ESTIMATED label, hidden when no open bets
- Live exposure updates every 30 seconds via existing polling
- Admin settlement cards show share-adjusted P&L from settlement_results in purple
- All five APNL requirements (07-11) are addressed across Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-p-l-views/06-02-SUMMARY.md`
</output>

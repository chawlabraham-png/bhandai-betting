---
phase: 06-agent-p-l-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [agent.html]
autonomous: true
requirements: [APNL-07, APNL-08, APNL-09]

must_haves:
  truths:
    - "Agent P&L summary shows actual settled totals from settlement_results (not estimates)"
    - "Agent can expand a settled market row to see per-market detail (client P&L, commission, agent net)"
    - "Agent can see per-client detail showing each client's total P&L, commission received, and agent net per client"
  artifacts:
    - path: "agent.html"
      provides: "settlement_results data loading, rewritten renderPnL with summary + per-market + per-client views"
      contains: "settlement_results"
  key_links:
    - from: "agent.html refreshData()"
      to: "settlement_results table"
      via: "sb.from('settlement_results').select('*').eq('agent_id', currentUser.id)"
      pattern: "settlement_results"
    - from: "agent.html renderPnL()"
      to: "allSettlementResults"
      via: "summary stat computation from settlement_results data"
      pattern: "allSettlementResults"
---

<objective>
Replace the estimated P&L in the agent dashboard with actual settlement_results data, add per-market expandable detail rows, and add per-client aggregated detail.

Purpose: Agents currently see rough estimates computed client-side. This plan switches to authoritative settled P&L from the settlement_results table (populated by RPCs in Phase 5), adds per-market drill-down, and per-client performance aggregation.

Output: Rewritten renderPnL() in agent.html that displays actual settled data with expandable market rows and client detail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-agent-p-l-views/06-CONTEXT.md
@.planning/phases/05-agent-p-l-core/05-01-SUMMARY.md

<interfaces>
<!-- settlement_results table schema (from sql/005_settlement_results_table.sql) -->
Columns: id (UUID PK), event_id (UUID FK), agent_id (UUID FK),
  total_client_pnl NUMERIC(15,2), total_commission_paid NUMERIC(15,2),
  agent_pnl_share NUMERIC(15,2), agent_commission_share NUMERIC(15,2),
  agent_net_pnl NUMERIC(15,2), partnership_share_at_settlement DECIMAL(5,2),
  settled_at TIMESTAMPTZ, created_at TIMESTAMPTZ

<!-- Existing agent.html global state -->
currentUser — agent's betting_users row (has .id, .balance, .match_commission, .partnership_share)
myClients — array of client betting_users rows under this agent
allEvents — all events (has .id, .title, .status, .is_resolved, .winning_outcome)
allOutcomes — all outcomes (has .id, .event_id, .title, .current_price)
allOrders — orders for this agent's clients (has .user_id, .outcome_id, .total_cost, .shares, .status)
allTransactions — credit_transactions involving this agent or their clients

<!-- Existing HTML element IDs in P&L tab -->
pnlVolume, pnlComm, pnlNetReceived, pnlPosition — stat card value spans
pnlClientTable — per-client table tbody
pnlMarketsTable — per-market table tbody

<!-- Existing patterns -->
fmt(n, decimals) — number formatter
sanitize(str) — XSS helper
timeAgo(date) — relative time
showToast(msg, type) — toast notification
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Load settlement_results in refreshData and rewrite P&L summary stats</name>
  <files>agent.html</files>
  <read_first>
    - agent.html lines 840-877 (refreshData function)
    - agent.html lines 439-480 (P&L tab HTML)
    - agent.html lines 1186-1205 (renderPnL summary stat section)
    - sql/005_settlement_results_table.sql (schema reference)
  </read_first>
  <action>
1. Add `let allSettlementResults = [];` to the global variable declarations near the top of the script section (near `let allOrders = [];` etc).

2. In `refreshData()`, after the second Promise.all block (around line 870), add a query to load settlement_results:
```
const srRes = await sb.from('settlement_results').select('*').eq('agent_id', currentUser.id);
allSettlementResults = srRes.data || [];
```

3. Update the P&L tab HTML structure (lines 439-480):
   - Change stat card labels per D-02 and D-03:
     - Card 1 (accent-purple): label "Total Client P&L" → value from `sum(total_client_pnl)` across all settlement_results. stat-change: "Across settled markets". Keep id="pnlVolume".
     - Card 2 (accent-green): label "Commission Given" → value from `sum(agent_commission_share)`. stat-change: "Your share of commission cost". Keep id="pnlComm".
     - Card 3 (accent-blue): label "Agent P&L Share" → value from `sum(agent_pnl_share)`. stat-change: "Partnership share earnings". Keep id="pnlNetReceived".
     - Card 4 (accent-amber): label "Net Position" → value from `sum(agent_net_pnl)`. stat-change: "P&L minus commission". Keep id="pnlPosition".
   - Color-code Net Position: green if positive (#10b981), red if negative (#ef4444).

4. Rewrite the summary section of renderPnL() (lines 1186-1204) to compute from allSettlementResults per D-01, D-02:
```javascript
const totalClientPnl = allSettlementResults.reduce((s,r) => s + parseFloat(r.total_client_pnl || 0), 0);
const totalCommGiven = allSettlementResults.reduce((s,r) => s + parseFloat(r.agent_commission_share || 0), 0);
const agentPnlShare = allSettlementResults.reduce((s,r) => s + parseFloat(r.agent_pnl_share || 0), 0);
const netPosition = allSettlementResults.reduce((s,r) => s + parseFloat(r.agent_net_pnl || 0), 0);
```
Set the four stat card values using these computed totals. Color the net position value: green if >= 0, red if negative.

5. If allSettlementResults is empty, show "No settled markets yet" in the stat cards (display 0 values) — this is the natural fallback since reduce starts at 0.
  </action>
  <verify>
    <automated>grep -c 'settlement_results' agent.html | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - agent.html contains `allSettlementResults` global variable declaration
    - refreshData() queries `settlement_results` table filtered by agent_id
    - renderPnL() computes summary stats from allSettlementResults (not from allOrders estimates)
    - Stat card labels read: "Total Client P&L", "Commission Given", "Agent P&L Share", "Net Position"
    - Net Position value is color-coded green/red based on sign
  </acceptance_criteria>
  <done>Agent P&L summary tab shows actual settled totals from settlement_results table with correct labels and color coding</done>
</task>

<task type="auto">
  <name>Task 2: Per-market expandable detail and per-client aggregated detail</name>
  <files>agent.html</files>
  <read_first>
    - agent.html lines 1229-1251 (current settled markets table rendering)
    - agent.html lines 1206-1227 (current per-client table rendering)
    - agent.html lines 466-479 (pnlMarketsTable and pnlClientTable HTML)
  </read_first>
  <action>
1. **Per-Market Detail Table (APNL-08, per D-04, D-05):**

   Update the "Settled Market P&L" table header (around line 475-476) to:
   `<thead><tr><th></th><th>Market</th><th>Result</th><th>Client P&L</th><th>Commission</th><th>Agent Net</th><th>Share %</th></tr></thead>`
   (The first empty `<th>` is for the expand/collapse toggle arrow.)

   Rewrite the settled markets section of renderPnL() (lines 1229-1251):
   - Join allSettlementResults with allEvents: for each settlement result, find the matching event by event_id.
   - Render one `<tr>` per settlement result row, showing:
     - Toggle arrow (clickable, `onclick="toggleMarketDetail(this)"` or inline toggle)
     - Event title (from allEvents match, sanitized)
     - Result: winning_outcome or "VOID" badge (from the event)
     - total_client_pnl (color: green if positive meaning clients won, red if negative meaning clients lost)
     - agent_commission_share (purple #a78bfa)
     - agent_net_pnl (green if positive, red if negative)
     - partnership_share_at_settlement with "%" suffix
   - Below each main row, add a hidden detail `<tr class="pnl-detail-row" style="display:none;">` with a `<td colspan="7">` containing a mini-table of per-client breakdown for that market:
     - Query allOrders filtered by outcome_ids for that event, grouped by user_id
     - For each client: login_id, total staked (sum total_cost), total shares, P&L estimate (shares - total_cost for winners, -total_cost for losers based on winning_outcome)
     - Show commission transactions for that market if available from allTransactions (filter by notes containing event title and type COMMISSION)
   - Add a simple `toggleMarketDetail(el)` function that finds the next sibling tr.pnl-detail-row and toggles display:none/table-row, rotating the arrow.
   - If no settlement results exist, show empty state: "No settled markets yet".

2. **Per-Client Detail Table (APNL-09, per D-06, D-07):**

   Update the "Per-Client Performance" table header (around line 469) to:
   `<thead><tr><th>Client</th><th>Total Bets</th><th>Volume</th><th>Settled P&L</th><th>Commission Recv</th><th>Agent Net</th></tr></thead>`

   Rewrite the per-client section of renderPnL() (lines 1206-1227):
   - For each client in myClients:
     - Total bets: count of allOrders where user_id === client.id
     - Volume: sum of total_cost from those orders
     - Settled P&L: compute from settled orders. For each settled event the client bet on:
       - Find orders for that client on that event's outcomes
       - If order's outcome matches winning_outcome: P&L = shares - total_cost (profit)
       - If not: P&L = -total_cost (loss)
       - Sum across all settled events
     - Commission received: sum of COMMISSION transactions where receiver_id === client.id from allTransactions
     - Agent Net per client: (settled P&L * -1 * partnership_share/100) - (commission * partnership_share/100). Use the agent's current partnership_share since per-client detail aggregates across markets.
   - Color code: Settled P&L green if client lost (agent earns), red if client won (agent pays). Agent Net green if positive, red if negative.
   - If no clients, show empty state.

3. **Add CSS for expandable rows** (add near existing agent.html styles):
```css
.pnl-detail-row td { padding: 0 !important; }
.pnl-detail-row .detail-inner { padding: 8px 16px 12px 32px; background: rgba(15,23,42,0.5); }
.pnl-detail-row table { width: 100%; font-size: 0.78rem; }
.pnl-detail-row th { color: #64748b; font-weight: 500; padding: 4px 8px; }
.pnl-detail-row td { padding: 4px 8px; color: #cbd5e1; }
.pnl-toggle { cursor: pointer; transition: transform 0.2s; display: inline-block; }
.pnl-toggle.open { transform: rotate(90deg); }
```
  </action>
  <verify>
    <automated>grep -c 'pnl-detail-row\|toggleMarketDetail\|allSettlementResults' agent.html | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - Per-market table renders one row per settlement_results entry with event title, result, client P&L, commission, agent net, share %
    - Each market row has an expand toggle that reveals per-client breakdown for that market
    - Per-client table shows: client login_id, total bets, volume, settled P&L, commission received, agent net per client
    - Positive/negative values are color-coded green/red throughout
    - Commission values use purple (#a78bfa) color
    - Empty states display when no data available
    - CSS for expandable detail rows is present
  </acceptance_criteria>
  <done>Agent can expand any settled market to see per-client detail, and the per-client performance table shows aggregated settled P&L with agent net per client</done>
</task>

</tasks>

<verification>
1. Open agent.html in browser, log in as an agent
2. Navigate to P&L tab
3. Verify summary stats show actual values from settlement_results (not estimates)
4. Verify per-market table shows settled markets with correct agent net P&L
5. Click expand arrow on a market row — per-client detail appears
6. Verify per-client table shows aggregated P&L across all settled markets
7. Confirm all values are color-coded (green positive, red negative, purple commission)
</verification>

<success_criteria>
- Agent P&L summary reflects actual settlement_results data
- Per-market expandable detail shows client P&L, commission, agent net per market
- Per-client table aggregates across settled markets with agent net per client
- No estimated data shown in settled sections — all from settlement_results
</success_criteria>

<output>
After completion, create `.planning/phases/06-agent-p-l-views/06-01-SUMMARY.md`
</output>
